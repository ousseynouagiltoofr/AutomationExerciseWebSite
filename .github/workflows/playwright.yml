name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
   # timeout-minutes: 60
    runs-on: ubuntu-latest
    permissions:      
      contents: write     
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps


        
    - name: Run Playwright tests with Allure Report 
      run: npm run test:allure

    - name: Upload Allure results 
      uses: actions/upload-artifact@v4 
      with:
        name: allure-report
        path: allure-report  

    - name: Deploy Allure Report to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }} 
        publish_dir: ./allure-report 
    - name: Notify Slack # Envoie une notification Slack avec le lien vers le rapport Allure. (détails configuration à voir sur le fichier README)
      run: |
              curl -X POST -H 'Content-type: application/json' --data '{"text":"📊 *Le rapport Allure est maintenant disponible !* 🚀\n\nVous pouvez consulter le rapport complet en suivant ce lien : <https://ousseynouagiltoofr.github.io/AutomationExerciseWebSite/|Allure Report>"}' ${{ secrets.WEBHOOK_AUTOMATION_EXERCISE}}        